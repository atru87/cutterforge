services:
  - type: web
    name: cutterforge
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt gunicorn
    startCommand: gunicorn -w 1 --timeout 180 -b 0.0.0.0:$PORT app:app
    envVars:
      - key: GROQ_API_KEY
        sync: false
